{
  "name": "evo",
  "version": "1.0.17",
  "description": "",
  "main": "main.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/ButovIvan98/test-auto-update.git"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-builder build --win --publish never",
    "deploy": "electron-builder build --win --publish always"
  },
  "author": "Ivan Butov",
  "license": "ISC",
  "devDependencies": {
    "electron": "^19.0.7",
    "electron-builder": "^23.1.0"
  },
  "dependencies": {
    "electron-updater": "^5.3.0"
  }
}
